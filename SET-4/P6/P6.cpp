#include <bits/stdc++.h>
#include "splay.cpp"

typedef long long ll;
typedef long double ld;
using namespace std;

void solve() {

}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int t = 1;
  // cin >> t;
  while (t > 0) {
    solve();
    --t;
  }
}
